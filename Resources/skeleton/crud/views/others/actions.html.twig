{%- for action in record_actions %}
    <a href="{{ "{{ path('"~ route_name_prefix ~"_"~ action ~"', { 'id': entity.id }) }}" }}" class="btn btn-primary button-{{ action|lower }}">{{ '{% trans %}' }}crud.{{ action|lower }}{{ '{% endtrans %}' }}</a>
{%- endfor %}
